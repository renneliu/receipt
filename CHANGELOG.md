# 更新日志

本文件记录每个对外版本的改动。发新版本前在这里写一行说明。

## 1.15.0 (build 20)

- 流水号：模板检查器可选择是否在两侧加 `*`（`* 1 2 3 *` / `1 2 3`），预览与 IMAX / Ritz 打印一致
- 文本换行：日期、流水号等支持「允许换行」；换行内容按元素框裁剪，并与二维码并排布局兼容
- 画布预览：流水号按打印 HRI 样式（含可选星号与字间距）显示

## 1.14.0 (build 19)

- 影票模板：可调背景图 X/Y 位置与缩放；编辑画布显示背景
- 二维码 / 条码：内容来源可选「流水号」或「自定义」；自定义时主页面出现对应输入框，打印路径按设置取内容
- 占位框：框尺寸即打印区域；画布缩放不再误改字号/打印倍率（独立 `printHeightScale`）；文字支持框内换行与单行裁切
- IMAX / 自定义布局：画布上与二维码并排的文字可同条打印（合成 GS v 0 位图条带）；二维码尺寸与横向位置与预览对齐
- PDF 识别：开始时间支持 24 小时制（如 `18:30`）
- 发版流程：`release.sh` 同步 `ReceiptPrinterStore.xcodeproj` 与 `ReceiptPrinterStore.app`

## 1.13.0 (build 16)

- Orpheum：影厅反色黑框宽度跟随占位框「宽」；「打印宽/高」1×/2×/3× 独立控制数字倍率
- Orpheum / ESC/POS：Cinema 与影厅打印倍率分离；原生打印与预览均按模板字号输出
- IMAX：影厅默认按识别原文显示（如 IMAX 1），不再改写成 Cinema 1
- 模板编辑：占位框最小宽降至 12；宽/高可用步进调节；Orpheum 双联对应元素同步

## 1.12.0 (build 15)

- 界面中英文切换（设置 → 软件语言）；影票 / POS / 快速打印等主要文案走统一本地化
- 影票：恢复 TMDB「联网核查」（独立查询框、片名清洗、片长/分级用途分流）；分级开关与打印映射
- 影票 / 快速打印 / Excel / PDF：打印快捷键 ⌘↩；核查框支持回车搜索
- 应用图标：打包 `AppIcon`，设置「关于」与 Dock 使用同一 Logo

## 1.11.0 (build 14)

- 影票：支持一张订单多张票（票数、分票座位区与流水号后缀）
- 模板：新建为空画布；Ritz / IMAX / 自定义布局分流；删除模板需确认
- 影厅：显示模式（Cinema+数字 / 仅数字 / 自定义前缀 / 识别原文）；反色与同行文字可同排打印
- PDF 导入：按关键字优先匹配模板；字段识别器去重；旧模板缺字段仍可加载（不再静默消失）
- 片名单行裁切在 IMAX 路径生效；调试包构建后自动 ad-hoc 签名

## 1.10.0 (build 13)

- 新增 IMAX SYDNEY 影票模板：捆绑 Logo、按画布 Y 排序的原生 ESC/POS 打印与预览
- 模板编辑：撤销 / 默认布局、⌘⇧ 多选与批量移动、方向键微调、未保存离开提示、复制模板（原名(1)）
- 条码高度可自由设定并保存；票型+票价同行，左右位置跟随画布 X
- 修复打印预览 Logo 上下颠倒（`respectFlipped`）；新增「切纸前走纸」控制票尾长度
- Logo 支持按纸宽比例放大；IMAX 条码模块宽度与间距微调

## 1.9.0 (build 12)

- 新增影票打印：双联 Ritz 风格票面、电影信息录入、PDF 订单识别与打印记录
- 修复打印任务首包丢失造成的上联缺字；使用 Font A 校准票面比例、条码宽度与单行片名裁切
- 模板编辑器支持元素位置、尺寸、粗体、对齐及 1×/2×/3× 原生打印字号
- 编辑画布改为与打印块尺寸一致的占位符，并提供 ESC/POS Font A 打印预览
- 影票主页右侧改为上下布局：上方实时预览，下方分页模板选择

## 1.8.0 (build 11)

- POS：打印记录（载入条目、重打、导出 PDF、删除/清理）
- POS：打印成功后自动清空条目；附加费比例注释 `(10%)`；「总计」汇总元素（条目数）
- POS：主页刷新已上传 Excel；非沙盒下 bookmark 解析回退，修复「格式不正确」
- POS：导出 PDF 方向修正（PDFKit）；打开默认落在主页面
- 模板：元素列表 / 显示名称 / 带样式复制；文本类元素统一支持左、中、右对齐
- 主页布局：表单可滚动，Excel 快捷区不再挡住操作；去掉易吞点击的透明快捷键层

## 1.7.0 (build 10)

- 新增 POS 小票：模板设计、条目录入、预览与打印
- 项目名称按框宽换行（英文整词），可调「宽（换行）」并压缩数量/金额列
- 非行字段可设「头部/尾部」，条目增多时尾部随列表下移
- POS 打印走 WYSIWYG 分条光栅，保留字号与布局
- 修复连续打印第 2 张起头部乱码（切纸后退出中文模式）
- 按墨迹高度裁切票面，改善切刀位置

## 1.6.0 (build 9)

- 快速打印：完整支持背景图与多 Logo（拖拽/缩放，草稿独立保存）
- 快速打印：自动编号（字号选单、位置、起始值、批量张数；打印后起始值按张数递增）
- 打印路径：中文与编号走原生 GBK；Logo 按垂直位置插入紧凑光栅条，避免整页位图乱码与 Logo 永远在票顶
- Excel 序列页同步采用上述安全混排策略

## 1.5.0 (build 8)

- Excel 序列：支持多个 Logo（拖拽/缩放）、背景缩放比例；彩图导入时二值化便于热敏打印
- 长票编辑：画布高度随软换行与叠加层增长，可滚到最底部；光标跟随滚动
- 有 Logo/背景时整页软件合成后分条 `GS v 0` 打印，Logo 可与文字同一行且避免整页光栅乱码
- 无媒体时仍走原生 GBK 文本路径（与快速打印一致）

## 1.4.0 (build 7)

- Excel 序列打印：支持背景图（文字下、等比居中）与可拖缩放 Logo
- 背景/Logo 参与软件合成位图打印（`initializeForRaster`），避免旧中文模式混打乱码
- 修复 flipped 画布下 Logo/背景打印上下颠倒（`respectFlipped`）
- 导入 Excel/CSV 后不再自动添加全部占位框，改为侧栏手动添加
- 模板与草稿可存取背景图、Logo 与 Logo 框位置

## 1.3.0 (build 6)

- Excel序列打印：可拖动/缩放的列占位框、导入后自动建框、支持单独删除
- 序列打印改为每页位图 + 每项切纸，修复第 2 张起中文乱码
- 占位框与正文按纸宽字符网格合成，打印更接近编辑区布局
- 模板存取、侧栏序列行编辑与 CSV/XLSX 导入（含中文编码）一并完善

## 1.2.0 (build 5)

- 新增左侧「Excel表格序列打印」页；快速打印侧栏去掉文本框/导入表格
- 快速打印：格式工具移到右侧「文本格式」区，去掉颜色选择；走纸行数仅保留输入框
- 走纸/切纸改走 `PrintController` 串行，修复先走纸再打印乱码（去掉走纸中的 `FS .` 与空格填充）
- 富文本编辑：粘贴为纯文本、锁定横向滚动、草稿存储可分文件
- CUPS 小任务填充改为重复 `ESC @`，避免尾随空格干扰后续作业

## 1.1.0 (build 4)

- 快速打印：本机 POS-80 改回 GBK 原生文本路径（整页 GS v 0 在该机上易被当成文字打成乱码）
- 新增「打印诊断」：每次打印后台保存作业 ID 与载荷产物，可对比两次作业
- 打印传输：`PrintController` 串行、脱离主线程，避免打印时界面卡住
- 快速打印默认空白草稿；去掉打印后强制标记结果条
- 富文本编辑、表格序列打印、邮件抓取规则、TMDB 片长等能力一并纳入本版

## 1.0.2 (build 3)

- Gmail 同步、Orpheum 邮件解析与电影票编辑

## 1.0.1 (build 2)

- 修复电影票页眉打印，预览与 ESC/POS 布局统一

## 1.0.0 (build 1)

- 首个可用版本：CUPS 热敏打印、模板设计、电影票模板、Gmail 订单、小票 OCR
